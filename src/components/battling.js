// poke / evs / moveset
// sprite overlaying 'rectangle' in battle frame with name(gender sign? and level?)/evs/moves
const battling = () => {
    return (
        <>
        </>
    )
}

export default battling;